#include<cuda.h>
#include<stdio.h>
#include<ctime>
using namespace std;
/*Base configuration of normal cuda script -> try this to set your configuration*/
/*To run this I have access on workstation university where there are code cuda configuration*/
/*I use MobaXTerm to create the ssh connection and visual studio code or nano tool to modify the code*/

int main()
{
    printf("Configuration completed? Yes my friend");
    return 0;
}